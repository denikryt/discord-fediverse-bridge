services:
  bridge:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_VERSION: ${BRIDGE_VERSION:?BRIDGE_VERSION is required}

  fedify-gateway:
    build:
      context: .
      dockerfile: fedify-gateway/Dockerfile
      args:
        APP_VERSION: ${BRIDGE_VERSION:?BRIDGE_VERSION is required}
